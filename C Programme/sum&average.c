#include<stdio.h>
#include<conio.h>
main()
{
    int a, b, c,sum;
    printf("Enter three numbers");
    scanf("%d %d %d", &a, &b, &c);
    sum= a + b + c
    printf("sum is %d",sum);
    return 0;
    getch();
}